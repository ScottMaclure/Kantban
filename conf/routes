# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                       Application.index

# Project pages
GET     /project/{id}/board                     Project.board
GET     /project/{id}/process                   Project.process
GET     /project/{id}                           Project.index

# Ajax calls
GET     /ajax/move/story-{storyId}/to/i-{index}                    State.moveStory
GET     /ajax/move/story-{storyId}/to/state-{stateId}/i-{index}    State.moveStoryToState

GET     /ajax/move/state-{stateId}/to/i-{index}                    Project.moveState

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

# Catch all
*       /{controller}/{action}                  {controller}.{action}
